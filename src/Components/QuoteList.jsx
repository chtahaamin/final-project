  // import React from 'react';
  // import './QuoteList.CSS'

  // const QuoteList = ({ quotes, setQuotes }) => {
  //   const handleDeleteBtn = (index) => {
  //     const updatedQuotes = quotes.filter((quote, i) => i !== index);
  //     setQuotes(updatedQuotes);
  //   };

  //   return (
  //     <ul className="quote-list">
  //       {quotes && quotes.length > 0 ? (
  //         quotes.map((quote, index) => (
  //           <li key={index} className="quote-item">
  //             {quote.quote}
  //             <button onClick={() => handleDeleteBtn(index)} className="delete-btn">
  //               Delete Quote
  //             </button>
  //           </li>
  //         ))
  //       ) : (
  //         <li className="no-quotes">No quotes available</li>
  //       )}
  //     </ul>
  //   );
  // };

  // export default QuoteList;
