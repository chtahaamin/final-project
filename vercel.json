{
  "version": 2,
  "builds": [
    { "src": "*.js", "use": "@vercel/node" }
  ],
  "routes": [
    { "src": "/signup/(.*)", "dest": "/signup" },
    { "src": "/login/(.*)", "dest": "/login" },
    { "src": "/(.*)", "dest": "/" },
    { "src": "(.*)", "dest": "/" }
  ]
}
